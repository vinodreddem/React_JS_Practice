const hobbies = ['A', 'B', 'C', 'D'];
for(const h in hobbies){
    console.log(h)
}

// Traditional way of for loop
for (let step = 0; step < 5; step++) {
    // Runs 5 times, with values of step 0 through 4.
    console.log("Walking east one step", step);
  }